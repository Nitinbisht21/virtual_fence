# Geofence Map Builder - Python Backend Requirements
#
# Supports both:
#   1. MongoDB & MongoDB Compass (primary persistent database)
#   2. Local SQLite (geofences.db) dual-fallback
#
# To install:
#   python -m venv venv
#   .\venv\Scripts\activate   # Windows
#   pip install -r requirements.txt
#
Flask>=2.2.0
pymongo>=4.0.0
python-dotenv>=1.0.0
